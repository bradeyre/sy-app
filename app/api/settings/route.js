import{NextResponse}from"next/server";import{query}from"@/lib/db";export const dynamic="force-dynamic";export async function GET(){try{const{rows}=await query(`select consignment_pct, voucher_pct, accessory_bonus, condition_faults,
free_text_fault_categories, ai_deduction_categories, max_deduction_pct
from calc.public_settings limit 1`);if(rows.length===0)return NextResponse.json({error:"Settings not configured"},{status:500});const row=rows[0];return NextResponse.json({consignmentPct:Number(row.consignment_pct),voucherPct:Number(row.voucher_pct),accessoryBonus:row.accessory_bonus||{},conditionFaults:row.condition_faults||{},freeTextFaultCategories:row.free_text_fault_categories||[],aiDeductionCategories:row.ai_deduction_categories||[],maxDeductionPct:row.max_deduction_pct||{}})}catch(err){return console.error("GET /api/settings failed",err),NextResponse.json({error:"Could not load settings"},{status:500})}}